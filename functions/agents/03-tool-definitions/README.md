# Layer 3 — Tool definitions

See [`statusTools.js`](./statusTools.js) — 3 read-only tool schemas the
status assistant (`../05-agent-loop/statusAssistant.js`) can call. See
`../README.md`'s "3 — Tool definitions" and "Trigger scope" sections for
why these are read-only only, and what's deliberately NOT here (anything
that publishes, posts, or connects).
