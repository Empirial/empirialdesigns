# Layer 9 — Streaming and response delivery

Lives in `functions/index.js`'s `aiChat` handler (SSE setup + the
`onProgress` → `data: {...}` chunk envelope). See `../README.md`'s
"9 — Streaming and response delivery" section for exact line references.
